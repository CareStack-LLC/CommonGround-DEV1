# FastAPI Backend Requirements

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.0
psycopg2-binary>=2.9.9  # PostgreSQL driver
asyncpg>=0.29.0  # Async PostgreSQL

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<5.0.0  # Pin to 4.x for passlib compatibility
python-multipart>=0.0.6
bleach>=6.3.0  # Input sanitization for XSS protection

# Supabase
supabase>=2.3.0
postgrest>=0.13.0

# AI/ML (ARIA)
anthropic>=0.18.0
openai>=2.14.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0
redis>=5.0.1
pywebpush>=1.14.0  # Web Push notifications

# Data Processing
pypdf>=4.0.0  # PDF generation
pymupdf>=1.23.0 # PDF extraction (fitz)
reportlab>=4.0.9  # PDF creation
weasyprint>=60.0  # HTML-to-PDF for branded reports
python-docx>=1.1.0  # Document generation

# Payments
stripe>=7.10.0

# Monitoring & Logging
sentry-sdk[fastapi,anthropic,openai,sqlalchemy,httpx]>=2.44.0  # Logs, Metrics, AI monitoring, DB spans
structlog>=24.1.0

# Testing
pytest>=7.4.4
pytest-asyncio>=0.23.3
pytest-cov>=4.1.0
fakeredis>=2.26.0  # in-memory Redis for circuit breaker / ai_usage tests
# End-to-end orchestrated test deps (tests/e2e/test_full_system_e2e.py):
#   pytest-order enforces @pytest.mark.order sequencing
#   pytest-dependency skips downstream stages when an upstream one fails
#   pytest-html produces the launch-day HTML report
pytest-order>=1.2.1
pytest-dependency>=0.6.0
pytest-html>=4.1.1
httpx>=0.26.0  # For testing API calls

# Code Quality
black>=24.1.0
ruff>=0.1.14
mypy>=1.8.0

# Templating
jinja2>=3.1.2

# Email
sendgrid>=6.11.0

# Scheduler (background jobs — Daily.co room cleanup, etc.)
apscheduler>=3.10.0

# Image processing for chore-proof photos
# Pillow handles JPEG/PNG/WebP decoding + EXIF stripping. pillow-heif plugs
# HEIC support into Pillow so kid photos taken on iPhone also get stripped.
Pillow>=11.0.0
pillow-heif>=1.3.0
